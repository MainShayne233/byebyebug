require 'spec_helper'

describe Byebyebug do
  it 'has a version number' do
    expect(Byebyebug::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
